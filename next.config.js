/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["assets.aceternity.com", "cdn.prod.website-files.com", "res.cloudinary.com"],
  }
}

module.exports = nextConfig;
